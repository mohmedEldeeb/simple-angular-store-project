export interface productCart {
    howMany:number,
    product:{
        description: string,
        id: number,
        name:string ,
        price: number,
        url: string
    }
}

export interface Prodrct {
    description: string,
    id: number,
    name:string ,
    price: number,
    url: string
}